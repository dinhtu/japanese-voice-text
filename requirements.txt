# Everything needed to run the pronunciation API + practice page and the tests.
#
#   python -m venv .venv
#   .venv\Scripts\activate          # macOS/Linux: source .venv/bin/activate
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements.txt
#   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
#
# pyopenjtalk has no wheels on PyPI - it compiles from source on EVERY platform.
# Install a C/C++ toolchain + CMake first or pip dies on it:
#   Ubuntu/Debian  sudo apt update && sudo apt install -y python3.14-venv build-essential cmake python3-dev
#   Fedora/RHEL    sudo dnf install -y gcc gcc-c++ make cmake python3-devel
#   macOS          xcode-select --install  &&  brew install cmake
#   Windows        "Visual Studio Build Tools" + "Desktop development with C++"
#
# Linux x86_64: the PyPI torch wheel pulls ~16 nvidia-* CUDA packages (several GB)
# even with no GPU. On a CPU-only box, install torch first from the CPU index:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

# Web API + server-rendered practice page
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18
jinja2>=3.1.4
pydantic>=2.9.0
python-dotenv>=1.0.0

# ASR model + audio decoding
torch>=2.6.0
torchaudio>=2.6.0
transformers>=4.47.0
numpy>=1.26.0
soundfile>=0.12.0
pyopenjtalk>=0.4.1

# Tests (pytest tests/)
pytest>=8.0
httpx>=0.27.0

# /coach: natural-language Vietnamese coaching comments via a locally-run
# Ollama model (https://ollama.com). Optional -- the rest of the API works
# fine without it, /coach just returns HTTP 503 until this is set up:
#   1. Install Ollama and start it:  ollama serve
#   2. Pull a model:                 ollama pull qwen3:8b
#   3. pip install this package (below), then set OLLAMA_MODEL in .env if
#      you picked a different model.
ollama>=0.4.0

# Fluency VAD (tiny, CPU). Optional -- energy-gate fallback if missing.
silero-vad>=5.1.0

# PASQA intonation (vendor/pasqa + models/pasqa weights).
# s3prl pulls a wav2vec2-base SSL the first time PASQA loads (~360MB).
# 0.4.10 crashes on Python 3.11 (mutable dataclass default). 0.4.18 is OK.
pyyaml>=6.0
s3prl==0.4.18

# Training / dataset tooling for scripts/ — NOT needed to serve the API.
# Uncomment the ones you need before training or running the realtime demos.
# datasets>=3.0.0,<4.0.0
# torchcodec>=0.10.0
# wandb>=0.19.0
# sounddevice>=0.5.5     # scripts/realtime_asr*.py (microphone input)
# unidic>=1.1.0          # python -m unidic download
# gdown>=5.2.1           # scripts/00d_prepare_jvs.py (JVS download)
# anthropic>=0.84.0      # scripts/realtime_asr_claude_compare.py only
